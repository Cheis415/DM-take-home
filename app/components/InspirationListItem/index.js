import styled from 'styled-components';

const InspirationListItem = styled.li`
  background: lightgray;
  border: 1px solid black;
  border-radius: 3px;
  margin-bottom: 3px;
`;

export default InspirationListItem;
